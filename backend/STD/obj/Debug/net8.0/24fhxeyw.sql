CREATE TABLE IF NOT EXISTS "__EFMigrationsHistory" (
    "MigrationId" character varying(150) NOT NULL,
    "ProductVersion" character varying(32) NOT NULL,
    CONSTRAINT "PK___EFMigrationsHistory" PRIMARY KEY ("MigrationId")
);

START TRANSACTION;

CREATE TABLE "Todo" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "Title" varchar NOT NULL,
    "Finished" boolean NOT NULL DEFAULT FALSE,
    "FinishedAt" timestamp without time zone,
    "CreatedAt" timestamp without time zone NOT NULL,
    CONSTRAINT "PK_Todo" PRIMARY KEY ("Id")
);

INSERT INTO "Todo" ("Id", "CreatedAt", "Finished", "FinishedAt", "Title")
VALUES (1, TIMESTAMP '2024-04-01T00:00:00', TRUE, TIMESTAMP '-infinity', 'Comprar maçãs');

SELECT setval(
    pg_get_serial_sequence('"Todo"', 'Id'),
    GREATEST(
        (SELECT MAX("Id") FROM "Todo") + 1,
        nextval(pg_get_serial_sequence('"Todo"', 'Id'))),
    false);

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20240421141303_init_db', '8.0.4');

COMMIT;

